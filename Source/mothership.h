#pragma once
#include <stdlib.h>     /* srand, rand */
#include <time.h>       /* time */
#include"Enemy.h"
class MotherShip //: Enemy 
{
public:
	/*MotherShip(int i, Vector2 pos, std::shared_ptr<ASGE::Renderer> renderer);
	float GetshipY();
	float GetshipX();
	float GetWidth();
	float Gethight();
	virtual void Render(std::shared_ptr<ASGE::Renderer> renderer);

	bool init(std::shared_ptr<ASGE::Renderer> renderer);
	bool hasHitWall();
	void MoveShip();
	bool setRandomSide();
	bool getSpawnded();
	void setSpawnded(bool hasbeenSpawnded);
	bool deleteMotherShip();
private:
	float Xpos;
	float Ypos;
	float Width;
	float Height;
	bool movingRight;
	bool Spawnded;
	std::unique_ptr<ASGE::Sprite> Mship;*/

};